[project]
name = "repolens"
version = "0.1.0"
description = "Bounded public GitHub repository ingestion for RepoLens"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
]
